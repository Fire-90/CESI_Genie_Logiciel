services:
  easyserver:
    build: .
    container_name: easyserver_app
    ports:
      - "11000:11000"
    environment:
      - DOTNET_RUNNING_IN_CONTAINER=true
    volumes:
      - ./data:/App/data
    restart: unless-stopped