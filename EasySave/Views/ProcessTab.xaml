<UserControl x:Class="Graphic.Views.ProcessTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Background="Transparent">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Suivi des autres utilisateurs (Serveur)" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
            <Button Content="Actualiser les etats" Command="{Binding RefreshProcessesCommand}" Padding="15,8" Background="#3498DB" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right"/>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="2"/>
            </Border.Effect>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding RemoteStates}">

                    <ItemsControl.Style>
                        <Style TargetType="ItemsControl">
                            <Style.Triggers>
                                <Trigger Property="HasItems" Value="False">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate>
                                                <TextBlock Text="Aucun autre utilisateur connecte ou aucun etat disponible." 
                                                           Foreground="#7F8C8D" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,20,0,0"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ItemsControl.Style>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Expander Header="{Binding ClientId}" IsExpanded="True" Margin="0,0,0,15" Background="#F8F9FA" BorderBrush="#BDC3C7" BorderThickness="1">
                                <DataGrid ItemsSource="{Binding Jobs}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10" HeadersVisibility="Column" Background="White" RowBackground="White" AlternatingRowBackground="#F2F2F2" BorderBrush="#E0E0E0">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Travail" Binding="{Binding Name}" Width="*"/>
                                        <DataGridTextColumn Header="Etat" Binding="{Binding State}" Width="*"/>
                                        <DataGridTextColumn Header="Progression" Binding="{Binding Progression, StringFormat={}{0:F0}%}" Width="*"/>
                                        <DataGridTextColumn Header="Fichiers (Restants/Total)" Width="1.5*">
                                            <DataGridTextColumn.Binding>
                                                <MultiBinding StringFormat="{}{0} / {1}">
                                                    <Binding Path="NbFilesLeftToDo" />
                                                    <Binding Path="TotalFilesToCopy" />
                                                </MultiBinding>
                                            </DataGridTextColumn.Binding>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header="Derniere Action" Binding="{Binding LastActionDate}" Width="1.5*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Expander>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>